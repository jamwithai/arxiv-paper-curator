# Weekly Review

每周 WNN D06 完成一次。评分必须引用测试、运行观察、代码 diff 或口头解释，不凭“感觉不错”打分。

## Week <N> — <YYYY-MM-DD>

### Weekly gate

- Gate statement: <从 CURRICULUM.md 复制本周验收标准>
- Result: <pass | partial | fail>
- Evidence: <测试命令、结果、运行观察和提交范围>
- Unverified items: <没有则写 none>

### Mastery evidence

每项使用 0–3 分：

- `0`：未尝试或无法解释；
- `1`：在完整提示/模仿下完成；
- `2`：少量提示下完成并能解释；
- `3`：无提示独立完成，并能迁移到新例子。

| Dimension | Score | Evidence |
|---|---:|---|
| Independent implementation | <0–3> | <specific evidence> |
| Conceptual explanation | <0–3> | <specific evidence> |
| Behavioral test quality | <0–3> | <specific evidence> |
| Error diagnosis | <0–3> | <specific evidence> |
| Retention from earlier sessions | <0–3> | <specific evidence> |

### Repeated error patterns

- <错误模式、出现课次、纠正证据；没有则写 none>

### What I can now do without notes

- <可观察能力>

### What still requires help

- <具体能力及最高提示等级>

### Next-week adjustment

Choose one and justify it with evidence. After deciding, update `current-state.md` deterministically:

- `advance`: set `Current session` to the next planned session, `Status` to `ready`, and `Next action` to its diagnostic question;
- `reinforce`: keep `Current session`, set `Status` to `ready`, and name one bounded remedial exercise in `Next action`;
- `reduce scope`: keep the same gate, remove only a named nonessential activity, set `Status` to `ready`, and record the reduced activity in `Next action`;
- `revisit prerequisite`: set `Current session` to the named earlier session or prerequisite exercise, `Status` to `ready`, and state its evidence goal in `Next action`.

Never use a weekly decision to mark an unverified gate as passed.

Decision: <value>  
Reason: <evidence-based explanation>

### Next review date

<YYYY-MM-DD>
